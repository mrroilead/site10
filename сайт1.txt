```jsx
// /src/pages/index.jsx
import { motion } from "framer-motion";

const fadeUp = {
  hidden: { opacity: 0, y: 40 },
  visible: { opacity: 1, y: 0 }
};

export default function Home() {
  return (
    <div className="bg-white text-gray-900 font-sans">
      {/* HERO */}
      <section className="min-h-screen flex flex-col justify-center items-center text-center px-6">
        <motion.h1
          initial="hidden"
          animate="visible"
          variants={fadeUp}
          className="text-4xl md:text-6xl font-semibold max-w-4xl"
        >
          Оригинальная косметика из США и Кореи с доставкой по России
        </motion.h1>

        <motion.p
          initial="hidden"
          animate="visible"
          variants={fadeUp}
          className="mt-6 text-lg text-gray-600 max-w-2xl"
        >
          Хиты Sephora, TikTok и корейского beauty-рынка — без подделок, без переплат и без риска
        </motion.p>

        <p className="mt-4 font-medium">100% оригинал или возврат денег</p>

        <div className="mt-8 flex gap-4">
          <button className="px-6 py-3 bg-black text-white rounded-full">
            Смотреть каталог
          </button>
          <button className="px-6 py-3 border rounded-full">
            Бестселлеры
          </button>
        </div>
      </section>

      {/* SOCIAL PROOF */}
      <section className="py-20 px-6 max-w-6xl mx-auto">
        <h2 className="text-3xl font-semibold text-center">
          Нам доверяют
        </h2>
        <div className="grid md:grid-cols-3 gap-6 mt-10">
          {["Rare Beauty оригинал", "Быстрая доставка", "Реальный эффект"].map(
            (t, i) => (
              <motion.div
                key={i}
                initial="hidden"
                whileInView="visible"
                variants={fadeUp}
                className="p-6 border rounded-2xl"
              >
                <p className="text-gray-700">{t}</p>
              </motion.div>
            )
          )}
        </div>
      </section>

      {/* BESTSELLERS */}
      <section className="py-20 bg-gray-50 px-6">
        <h2 className="text-3xl font-semibold text-center">
          Хиты продаж
        </h2>

        <div className="grid md:grid-cols-4 gap-6 mt-10 max-w-6xl mx-auto">
          {["Rare Beauty", "Charlotte Tilbury", "Hourglass", "Jo Malone"].map(
            (item, i) => (
              <motion.div
                key={i}
                whileHover={{ scale: 1.05 }}
                className="bg-white p-6 rounded-2xl shadow-sm"
              >
                <div className="h-40 bg-gray-200 rounded-xl mb-4" />
                <h3 className="font-medium">{item}</h3>
                <p className="text-sm text-gray-500">Хит недели</p>
              </motion.div>
            )
          )}
        </div>
      </section>

      {/* BENEFITS */}
      <section className="py-20 px-6 max-w-5xl mx-auto">
        <h2 className="text-3xl text-center font-semibold">
          Почему BEAUTYVIBE
        </h2>

        <div className="grid md:grid-cols-2 gap-6 mt-10">
          {[
            "Только оригинальная продукция",
            "Прямые поставки",
            "Проверка каждого товара",
            "Быстрая доставка",
            "Редкие позиции",
            "Поддержка"
          ].map((b, i) => (
            <motion.div
              key={i}
              variants={fadeUp}
              initial="hidden"
              whileInView="visible"
              className="p-4 border rounded-xl"
            >
              {b}
            </motion.div>
          ))}
        </div>
      </section>

      {/* CATEGORIES */}
      <section className="py-20 bg-gray-50 px-6">
        <h2 className="text-3xl text-center font-semibold">Категории</h2>

        <div className="grid md:grid-cols-4 gap-6 mt-10 max-w-6xl mx-auto">
          {["Уход", "Макияж", "Волосы", "Парфюм"].map((c, i) => (
            <motion.div
              key={i}
              whileHover={{ scale: 1.05 }}
              className="h-40 bg-white rounded-2xl flex items-center justify-center shadow-sm"
            >
              {c}
            </motion.div>
          ))}
        </div>
      </section>

      {/* TRUST */}
      <section className="py-20 px-6 max-w-4xl mx-auto text-center">
        <h2 className="text-3xl font-semibold">Покупка без риска</h2>
        <p className="mt-6 text-gray-600">
          Гарантия возврата • Оригинальная упаковка • Поддержка
        </p>
      </section>

      {/* URGENCY */}
      <section className="py-20 bg-black text-white text-center">
        <h2 className="text-3xl font-semibold">
          Поставки ограничены
        </h2>
        <p className="mt-4 text-gray-300">
          Следующая партия через 1–2 недели
        </p>
        <button className="mt-6 px-8 py-3 bg-white text-black rounded-full">
          Заказать сейчас
        </button>
      </section>

      {/* FOOTER */}
      <footer className="py-10 text-center text-sm text-gray-500">
        BEAUTYVIBE © 2026
      </footer>
    </div>
  );
}
```
